// import Versions from './components/Versions'
// import electronLogo from './assets/electron.svg'

function App(): React.JSX.Element {
  // const ipcHandle = (): void => window.electron.ipcRenderer.send('ping')

  // TODO: Add phaser / threeJS canvas & add whatever we want here
  // In theory we could also add a couple of features in main/index.ts for screen capture & voice control that will enable us
  // to talk to tamago-llm, send captures of the screen & chat / get back responses somewhere (don't know if via voice again or via a chat window)
  return <>Hi from Tamago LLM!</>;
}

export default App;
